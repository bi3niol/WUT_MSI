@model IEnumerable<WUT_MSI.WebApp.MinimalRules.Helpers.MinimalRuleVM>

@{
    ViewBag.Title = "GetMinimalRules";
}

<h2>GetMinimalRules</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(m => m.Name)
        </th>
        <th></th>
    </tr>

@foreach (var rule in Model)
{
    <tr>
        <td>
            @Html.DisplayFor(m => rule.Name)
        </td>
        <td>
            @Html.DisplayFor(m => rule.Function)
        </td>
    </tr>
}

</table>
